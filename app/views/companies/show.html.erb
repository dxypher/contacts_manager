<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @company.name %>
</p>

<ul>
  <% @company.phone_numbers.each do |phone| %>
    <li><%= phone.number %> <%= link_to "edit", edit_phone_number_path(phone) %> | <%= link_to "delete", phone_number_path(phone), method: :delete %></li>
  <% end %>
</ul>

<ul>
  <% @company.email_addresses.each do |email_address| %>
    <li><%= email_address.address %><%= link_to 'edit', edit_email_address_path(email_address) %> | <%= link_to "delete", email_address_path(email_address), method: :delete %></li>
  <%end%>
</ul>

<%= link_to 'Add phone number', new_phone_number_path(contact_id: @company.id, contact_type: 'Company') %> |
<%= link_to 'Add email address', new_email_address_path(contact_id: @company.id, contact_type: 'Company'), id: "new_email_address" %> |
<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
